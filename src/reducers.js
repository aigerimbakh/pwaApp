// reducers.js

// Начальное состояние
const initialState1 = {
   // Начальные значения для редьюсера 1
 };
 
 const initialState2 = {
   // Начальные значения для редьюсера 2
 };
 
 // Редьюсеры
 export const reducer1 = (state = initialState1, action) => {
   switch (action.type) {
     // Обработка различных действий
     default:
       return state;
   }
 };
 
 export const reducer2 = (state = initialState2, action) => {
   switch (action.type) {
     // Обработка различных действий
     default:
       return state;
   }
 };
 